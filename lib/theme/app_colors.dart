import 'package:flutter/material.dart';

class AppColors {
  // --- GitHub Light Theme ---
  static const lightBg = Color(0xFFF6F8FA); // The classic light gray background
  static const lightSurface = Color(0xFFFFFFFF); // White cards
  static const lightBorder = Color(0xFFD0D7DE); // Subtle borders
  static const lightText = Color(
    0xFF24292F,
  ); // Dark gray text (not pitch black)
  static const lightPrimary = Color(0xFF0969DA); // GitHub Blue
  static const lightSecondary = Color(0xFF2DA44E); // "New Item" Green
  static const lightContainer = Color(0xFFEAEEF2);

  // --- GitHub Dark Theme ---
  static const darkBg = Color(0xFF0D1117); // Deep dark background
  static const darkSurface = Color(0xFF161B22); // Slightly lighter cards
  static const darkBorder = Color(0xFF30363D); // Dark borders
  static const darkText = Color(0xFFC9D1D9); // Light gray text
  static const darkPrimary = Color(0xFF58A6FF); // Bright Blue
  static const darkSecondary = Color(0xFF238636); // Darker Green
  static const darkContainer = Color(0xFF21262D);
}
